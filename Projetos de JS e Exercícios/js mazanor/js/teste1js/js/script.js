function soma(a, b) {
    let resultado = a + b
    document.getElemnetById('demo').innerHTML = resultado;
}

function somaDoisNumero(){
    let numero = parserInt(prompt("Digite um numero:"))
    let numero1 = parseInt(prompt('Digite um numero'))
    let soma = numero + numero1;
    alert("A soma dos valores corresponde a:" + soma)

    numero=20
    alert ("A soma dos valores corresponde a:" )
    ("A soma dos valores corresponde a:" + numero)

}

