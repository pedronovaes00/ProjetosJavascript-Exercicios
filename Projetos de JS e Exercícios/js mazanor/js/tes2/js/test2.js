function entrada() {
    let idade = parset(prompt('Qual a sua idade?'))

    if (idade > 18) {
        alert("Pode entrar na balada")
    } 
    // as chaves no javascript substituem a palavra "então" do visualg,(se if(idade>18) entao alert pode entrar)

}